#pragma once

/// @brief Print the instructions saved inside the RAM one by one.
void analyzeInstructions();

/// @brief Dump all the instructions inside a txt file.
void dumpInstructions(bool dumpOut, char** argv);